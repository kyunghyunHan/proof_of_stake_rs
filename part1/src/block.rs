pub struct Block {
    pub id:usize,
    pub hash:String,
    pub previuos_hash:String,
    pub timestamp:i64,
    // pub txn:Vec<Transaction>
}
impl Block{
    pub fn new(){
      println!("hello");
        }
}
